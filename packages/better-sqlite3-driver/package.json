{
  "name": "@sqlite-js/better-sqlite3-driver",
  "version": "0.0.1",
  "description": "",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "test": "pnpm build && vitest",
    "clean": "tsc -b --clean && tsc -b ./test/tsconfig.json --clean && rm -rf lib test/lib"
  },
  "exports": {
    ".": "./lib/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^11.1.2",
    "@sqlite-js/driver": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "typescript": "^5.9.3",
    "vitest": "^2.0.5",
    "@sqlite-js/driver-tests": "workspace:^"
  }
}
